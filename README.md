A fully serverless daily quote generator built on AWS Lambda, API Gateway, DynamoDB, EventBridge, CloudFront, and S3.
The system automatically fetches a motivational quote every 5 minutes, stores it, and serves it through a clean public UI hosted on CloudFront.
